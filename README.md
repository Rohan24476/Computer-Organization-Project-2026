# Computer-Organization-Project-2026
Current status - Simple Assembler.py that copies the correct binary code to the output. Only done to test command line input of arguments as well as the grading code.

Run "python3 src\main.py --no-sim --windows" in automatedTesting folder.